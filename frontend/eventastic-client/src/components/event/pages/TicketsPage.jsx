import { ScrollContainer } from "../../styles/layouts.styled"

const TicketsPage = ({ setCreateEvent }) => {
  return (
    <ScrollContainer hide height='100%' >
      TicketsPage
    </ScrollContainer>
  )
}

export default TicketsPage

